n = int(input("Enter number of rows: "))
for i in range(0, n):
  for spaces in range(n, i, -1):
    print(' ', end='')
  num = 1
  for col in range(0, i+1):
    print(num, end=' ')
    num = num * (i-col)//(col+1)
  print()
